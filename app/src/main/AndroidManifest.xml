<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.near.labs">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Near">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Near.Hide.Status.Bar"
            android:windowSoftInputMode="adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".activities.NFTActivity"
            android:exported="true"
            android:theme="@style/Theme.Near.Hide.Status.Bar">

        </activity>


        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true"
            android:label=""
            android:parentActivityName=".activities.NFTActivity"
            android:theme="@style/Theme.Near.White.Status.Bar">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.NFTActivity" />

        </activity>


    </application>

</manifest>